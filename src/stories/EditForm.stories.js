import React from 'react';

import { EditForm } from '../components/Form';

const meta = {
  title: 'EditForm',
  component: EditForm
}

export default meta;

export const Primary = () => <EditForm />;