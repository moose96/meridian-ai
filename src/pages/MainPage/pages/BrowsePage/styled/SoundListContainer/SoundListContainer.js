import { styled } from '@material-ui/core/styles';
import { Box } from '@material-ui/core';

const SoundListContainer = styled(Box)({
  marginLeft: '5%',
  marginRight: '5%'
});

export default SoundListContainer;