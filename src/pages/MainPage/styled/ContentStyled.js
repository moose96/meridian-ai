import { styled } from '@material-ui/core/styles';
import { Router } from '@reach/router';

const ContentStyled = styled(Router)({
  display: 'flex',
  flexDirection: 'column',
  flex: 1
});

export default ContentStyled;