import { styled } from '@material-ui/core/styles';
import { Box } from '@material-ui/core';

const MenuDialogWrapper = styled(Box)({
  backgroundColor: 'rgba(0, 0, 0, 0.6)',
});

export default MenuDialogWrapper;
