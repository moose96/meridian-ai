import { CardActions } from '@material-ui/core';
import { styled } from '@material-ui/core/styles';

const CardActionsStyled = styled(CardActions)({
  display: 'flex',
  justifyContent: 'space-around'
});

export default CardActionsStyled;